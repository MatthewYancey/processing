function setup() {
  createCanvas(370, 800);
  background(237, 227, 223);
  for (var i = 0; i < dates.length; i++){
      new Name(dates[i]);
      new Dash(dates[i]);
    }

    for (var i = 0; i < dates.length; i++){
    }

}
