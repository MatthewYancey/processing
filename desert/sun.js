function Sun() {

    this.setup = function () {
        fill(s_color);
        noStroke();
        circle(width * 2 / 3, height * 2 / 3 - 10, 132);
    }

    this.update = function () {
        fill(s_color);
        noStroke();
        circle(width * 2 / 3, height * 2 / 3 - 10, 132);
    }

    this.setup();
}
